# ML dependencies. Install with: pip install -r requirements-ml.txt
#
# Genuinely optional now. Nothing that can block or redact a prompt needs a
# model: detection is regex plus lookups against the document entity index.
# When installed, MiniLM does embed every prompt for the advisory similarity
# signal, which can only warn.
#
# These packages improve Knowledge Shield coverage rather than enable it:
#   sentence-transformers + faiss-cpu → the advisory topic-similarity signal
#   transformers (pulled in by the above) → NER over uploaded documents, which
#     is what indexes person and organisation names
#
# Without them the shield still blocks leaks, using regex identifiers only —
# names go unindexed. GET /api/v1/knowledge-shield/status reports which
# coverage is actually active via ner_enabled / similarity_ready.
sentence-transformers==3.3.0
scikit-learn==1.5.2
faiss-cpu==1.9.0
numpy==1.26.4
